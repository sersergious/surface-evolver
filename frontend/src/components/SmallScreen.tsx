export default function SmallScreen() {
  return (
    <div className="flex h-screen w-screen items-center justify-center bg-gh-bg-base px-6">
      <p className="text-center text-base font-semibold text-gh-text-primary font-sans">
        Surface does not currently support small screens
      </p>
    </div>
  )
}
